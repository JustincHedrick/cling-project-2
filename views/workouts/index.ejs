<%- include('../partials/browseHeader') %>

<% if (user) {%>
  <table id="list">
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% workouts.forEach(function(w) { %>
      <tr>
        <td><%= w.name %></td>
        <td>
          <a href="/workouts/<%= w._id %>">DETAILS</a>
        </form>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
    <%}%>
<%- include('../partials/footer') %>